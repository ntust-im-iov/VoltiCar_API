# 數據庫連接配置
# 示例: mongodb://user:password@host:port/?authSource=admin&ssl=false
DATABASE_URL=mongodb://your_username:your_password@your_host:your_port/?authSource=admin&ssl=false
VOLTICAR_DB=your_volticar_db_name
CHARGE_STATION_DB=your_charge_station_db_name

# API 服務配置
API_HOST=0.0.0.0
API_PORT=22000
API_ENV=development
PYTHONIOENCODING=utf-8

# 身份驗證
# 請生成一個安全的隨機密鑰
SECRET_KEY=your_secret_key_here

# Firebase 設定 (需要實際金鑰)
# 可以設置FIREBASE_CREDENTIALS_PATH指向金鑰檔案
# 或者直接設置FIREBASE_CREDENTIALS存放金鑰內容 (JSON 格式)
# FIREBASE_CREDENTIALS_PATH=./path/to/your/firebase-credentials.json
# FIREBASE_CREDENTIALS={"type":"service_account", "project_id": "your_project_id", ...}

# --- hMailServer SMTP Settings ---
# 請根據您的 hMailServer 設定修改以下值
SMTP_HOST=volticar.dynns.com
SMTP_PORT=465
SMTP_USER=service@volticar.dynns.com
SMTP_PASSWORD=REMOVED_PASSWORD
SENDER_EMAIL=service@volticar.dynns.com

# --- API Base URL ---
# 用於生成郵件中的驗證/重設連結，應指向您的後端 API 地址
# 請務必在生產環境中設定正確的值
API_BASE_URL=https://volticar.dynns.com:22000
